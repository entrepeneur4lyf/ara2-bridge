#pragma once

// The pinned TestAnalysis.h uses fixed-width integer names without including their declaration.
// Force this standard header for every translation unit instead of modifying the upstream SDK.
#include <cstdint>
